package com.dam.hashmaps;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;
public class OrdenarHashMap {
    public static void main(String[] args) {
        ArrayList<Integer> testSort = new ArrayList<>();
        testSort.sort(null);
        Collections.reverse(testSort);
        Comparator<Integer> comparator = Collections.reverseOrder();// Comparator es algo que me va a decir como compararlo
                                                                    // Comparable prepara la clase para la comparacion
                                                                    //SOS
    }
    public static void ordenarListaObj(){
        
    }
}